#define MAJORVERSION @Hardt_VERSION_MAJOR@
#define MINORVERSION @Hardt_VERSION_MINOR@
#define BUILDNO @Hardt_VERSION_BUILD@

#ifndef __HARDT_H
#define __HARDT_H

#include <stdio.h>
#include <sstream>
#include <fstream>

std::string getversion();

void HWriteLogMessage(std::string message);
void HWriteErrorMessage(std::string file, std::string line, std::string message);

#define HLog(msg) HWriteLogMessage(msg)
#define HError(msg) HWriteErrorMessage(std::string(__FILE__), std::to_string(__LINE__), msg)

void HInit(std::string logname);

#endif
